# 📋 ИТОГОВАЯ ДОКУМЕНТАЦИЯ: МОДУЛЬ «ОРГАНИЗАЦИИ» (CUSTOMERS)

## 🎯 Обзор модуля

Customer‑модуль — это комплексная система управления пользователями клиентской части платформы RedSpider. Включает школы, преподавателей и студентов с разными уровнями доступа и функциональностью.

## 🏗️ Архитектура модуля

### 1. Основные сущности:

```text
Customer (основная модель)
├── CustomerGroup (группы: школа/преподаватель/студент)
├── CustomerCompany (информация о компании)
├── CustomerGroupOption (настройки групп)
├── CustomerGroupRouteAccess (права доступа)
├── CustomerMessage (система сообщений)
└── CustomerActionLog (логи действий)
```

### 2. Типы пользователей:

| Тип | `group_id` | Описание | Особенности |
| --- | --- | --- | --- |
| Школа | 5 | Организация, создающая курсы | `organization_name`, `org_logo1`, `registration_fee` |
| Преподаватель | 6 | Создатель контента | Может принадлежать школе (`parent_id`, `school_id`) |
| Студент | 7 | Участник курсов | Реферальная система, кошелёк |

## 📊 Таблицы базы данных

### Основные таблицы:

- `so_customer` — основная информация;
- `so_customer_group` — группы пользователей;
- `so_customer_company` — информация о компаниях;
- `so_customer_group_option` — настройки групп;
- `so_customer_group_route_access` — права доступа;
- `so_customer_message` — сообщения и уведомления.

## 🔧 Функциональность

### 1. Регистрация и аутентификация

- Самостоятельная регистрация с выбором типа;
- Верификация email и телефона (Twilio OTP);
- OAuth‑интеграции (Facebook, Twitter);
- 2FA‑поддержка (TOTP).

### 2. Управление профилем

- Личная информация;
- Геолокация (координаты, адрес);
- Аватар и галерея;
- Настройки уведомлений.

### 3. Организационная структура

- Школы → Преподаватели → Студенты;
- Иерархические связи через `parent_id`;
- Управление доступом в рамках организации.

### 4. Система прав доступа

- Ролевая модель на основе групп;
- Контроль доступа к маршрутам;
- Наследование прав;
- Гибкая система разрешений (`Allow`/`Deny`).

### 5. Коммуникации

- Система внутренних сообщений;
- Email‑уведомления;
- Шаблоны сообщений с поддержкой перевода;
- Массовая рассылка уведомлений.

## 🛡️ Безопасность

### 1. Верификация:

- Email‑confirmation с уникальным ключом;
- Phone‑verification через SMS OTP;
- Двухфакторная аутентификация.

### 2. Защита данных:

- Хеширование паролей через `passwordHasher()`;
- Сериализация настроек групп;
- Кэширование прав доступа.

### 3. Контроль доступа:

- Проверка маршрутов через `hasRouteAccess()`;
- Исключение технических методов;
- Автоматическое обнаружение контроллеров.

## 💼 Бизнес‑логика

### Для школ (`group_id = 5`):

- Регистрационный взнос: `registration_fee`;
- Визовая поддержка: `visa_support`;
- Геолокация: координаты для карт;
- Юридическая информация: через `CustomerCompany`.

### Для преподавателей (`group_id = 6`):

- Привязка к школе: `parent_id`, `school_id`;
- Создание курсов: ограниченные права;
- Управление контентом: уроки, задания.

### Для студентов (`group_id = 7`):

- Реферальная система: `referal_code`, `refered_by`;
- Кошелёк: накопление бонусов;
- История покупок: купленные курсы.

## 🌍 Геолокация и интернационализация

### Геоданные:

- Координаты: JSON‑формат `{"lat": 0.0, "lng": 0.0}`;
- Адрес: страна, регион, город, улица;
- Определение языка: по IP с кэшированием.

### Локализация:

- Поддержка английского и русского;
- Переводы через функцию `t()`;
- Параметры перевода в сообщениях.

## 📧 Система уведомлений

### Типы сообщений:

- Системные: регистрация, верификация;
- Бизнес‑события: покупка курса, запрос места;
- Уведомления: новые сообщения, обновления;
- Массовые: broadcast‑сообщения.

### Особенности:

- Поддержка параметров перевода;
- Статусы сообщений (`unseen`/`seen`);
- Автоматическая разметка как прочитанное;
- Связь с действиями через `CustomerActionLog`.

## ⚙️ Настройки и конфигурация

### Настройки групп:

```php
// Примеры настроек
'system.customer_sending.quota'          // Квота отправки
'system.customer_campaigns.max_campaigns' // Макс. кампаний
'system.customer_lists.max_subscribers'   // Макс. подписчиков
```
## ⚙️ Настройки и конфигурация

### Хранение настроек:

- Сериализация сложных значений;
- Кэширование для производительности;
- Иерархия: система → группа → пользователь.

## 🔄 Интеграции

1. **С модулем курсов:**
    - Школы создают курсы;
    - Преподаватели управляют контентом;
    - Студенты покупают и проходят курсы.

2. **С e‑commerce:**
    - Регистрационный взнос;
    - Реферальные выплаты;
    - Промо‑коды из кошелька.

3. **С email‑маркетингом:**
    - Welcome emails;
    - Transactional emails;
    - Подписка на рассылки.

## 📈 Бизнес‑процессы

### Регистрация школы:

1. Выбор типа «School»;
2. Заполнение данных + загрузка логотипа;
3. Ввод информации о компании;
4. Верификация email и телефона;
5. Активация аккаунта;
6. Доступ к созданию курсов.

### Приглашение преподавателя:

1. Школа генерирует приглашение;
2. Преподаватель регистрируется с кодом школы;
3. Школа подтверждает привязку;
4. Преподаватель получает доступ к созданию курсов.

### Реферальная система:

1. Студент получает реферальный код;
2. Делится кодом с друзьями;
3. Друг регистрируется по коду;
4. Студент получает % от покупок друга;
5. Накопления конвертируются в промо‑коды.

## 🎯 Ключевые особенности

1. **Единая таблица для всех типов:** все пользователи в `so_customer` с разделением по `group_id`.
2. **Гибкая система прав:** автоматическое обнаружение маршрутов + ручная настройка.
3. **Полная верификация:** email + телефон + дополнительные проверки для школ.
4. **Мультиязычность:** полная поддержка переводов интерфейса и сообщений.
5. **Масштабируемость:** кэширование, сериализация, оптимизированные запросы.

## 📁 Структура файлов

```text
apps/common/models/
├── Customer.php # Основная модель
├── CustomerGroup.php # Группы
├── CustomerCompany.php # Информация о компании
├── CustomerGroupOption.php # Настройки групп
├── CustomerGroupRouteAccess.php # Права доступа
├── CustomerMessage.php # Сообщения
├── CustomerActionLog.php # Логи действий
├── CustomerPasswordReset.php # Сброс пароля
├── CustomerAutoLoginToken.php # Токены автологина
└── CustomerForTwoFactorAuth.php # 2FA модель
```
## 🔍 Примеры использования
### Создание школы администратором:
```php
$school = new Customer();
$school->attributes = [
    'first_name' => 'Admin',
    'last_name' => 'School',
    'email' => 'info@school.com',
    'phone' => '+971501234567',
    'group_id' => 5,
    'organization_name' => 'International School Dubai',
    'registration_fee' => 500.00
];
$school->save();

// Добавление информации о компании
$company = new CustomerCompany();
$company->attributes = [
    'name' => 'International School Dubai LLC',
    'country_id' => 229, // UAE
    'address_1' => 'Sheikh Zayed Road',
    'city' => 'Dubai',
    'zip_code' => '12345',
    'vat_number' => 'VAT123456789'
];
$company->customer_id = $school->customer_id;
$company->save();
```
### Проверка прав доступа:
```php
// В контроллере
public function actionCreate()
{
    if (!$this->customer->hasRouteAccess('courses/create')) {
        throw new CHttpException(403, 'Access denied');
    }
    // ... создание курса
}
```
### Отправка уведомления:
```php
$message = new CustomerMessage();
$message->title = 'New Course Enrollment';
$message->message = 'Student {studentName} enrolled in course {courseTitle}';
$message->message_translation_params = [
    '{studentName}' => $student->getFullName(),
    '{courseTitle}' => $course->title
];
$message->customer_id = $instructor->customer_id;
$message->save();
```