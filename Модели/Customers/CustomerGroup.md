## 👥 Группы клиентов (Customer Groups)

### Таблица: `so_customer_group`

| Поле | Тип | Описание |
|------|-----|----------|
| `group_id` | INT | Уникальный идентификатор |
| `name` | VARCHAR(255) | Название группы |
| `plural_name` | VARCHAR(255) | Название во множественном числе |
| `is_default` | ENUM | Группа по умолчанию для регистрации |
| `require_approval` | ENUM | Требует одобрения администратора |
| `date_added` | DATETIME | Дата создания |
| `last_updated` | DATETIME | Дата обновления |

### Предопределенные группы (hardcoded в Customer.php):

| ID | Название | Описание |
|----|----------|----------|
| 5 | Школа (School) | Организации, создающие курсы |
| 6 | Преподаватель (Instructor) | Создатели контента, могут работать в школе |
| 7 | Студент (Student) | Покупатели и участники курсов |

### Функциональность групп:

#### 1. Управление настройками
```php
// Получение значения настройки
$group->getOptionValue('system.customer_sending.quota', 1000);

// Настройки хранятся в: so_customer_group_option
```
#### 2. Копирование групп
- Полное копирование со всеми настройками
- Копирование привязанных серверов доставки
- Автоматическое переименование

#### 3. Права доступа
```php
// Проверка доступа группы к маршруту
$group->hasRouteAccess('courses/create');

// Права хранятся в: so_customer_group_route_access
```

#### 4. Статистика и квоты
```php
// Количество клиентов в группе
$group->customersCount

// Сброс квот отправки
$group->resetSendingQuota()
```

#### 5. Связи с другими модулями
- Тарифные планы: `pricePlans (PricePlan)`
- Серверы доставки: `deliveryServers (DeliveryServer)`
- Клиенты: `customers (Customer)`

### Бизнес-логика:
**Для школ (group_id = 5):**
- require_approval = возможно 'yes' для проверки администратором
- Высокие лимиты на создание курсов
- Доступ к управлению преподавателями

**Для преподавателей (group_id = 6):**
- Может требовать одобрения школы
- Лимиты на создание курсов
- Доступ только к своим курсам

**Для студентов (group_id = 7):**
- Нет прав на создание контента
- Доступ только к купленным курсам
- Реферальная система

### Интерфейсные особенности:
- plural_name - используется в интерфейсе ("Schools", "Instructors", "Students")
- Сортировка: студенты всегда первыми (group_id="7" desc)
- Выпадающие списки через getGroupsArray()

