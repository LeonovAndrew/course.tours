# Redspider Solution - Образовательная платформа

## 📌 Общая информация

**Проект:** Redspider Solution (образовательная платформа)  
**Версия:** 1.0  
**Фреймворк:** Yii 1.1  
**Архитектура:** Multi-tenant (админка + клиентская часть)  
**Основной функционал:** Курсы, статьи, email-маркетинг, e-commerce

---

## 🏗️ Архитектура проекта

### 2.1. Структура каталогов
apps/
├── customer/ # Клиентская часть (frontend)
│ ├── components/
│ ├── controllers/
│ ├── models/
│ ├── views/
│ └── config/
├── backend/ # Админ-панель
└── console/ # Консольные команды

common/ # Общие компоненты
data/ # Данные, миграции
themes/ # Темы оформления
vendor/ # Сторонние библиотеки


### 2.2. Конфигурационные файлы

- `apps/customer/config/main.php` — конфигурация клиентской части
- `apps/backend/config/main.php` — конфигурация админки
- `common/config/` — общие настройки

---

## 🗄️ База данных

### 3.1. Основные таблицы

| Таблица | Описание |
|---------|----------|
| `so_course` | Курсы |
| `so_article` | Статьи/страницы |
| `so_customer` | Клиенты/пользователи |
| `so_shop_order` | Заказы |
| `so_campaign` | Email-рассылки |
| `so_list` | Списки подписчиков |
| `so_services` | Услуги (проживание, трансфер и т.д.) |

---

## 🎮 Контроллеры и маршруты

### 4.1. Основные контроллеры (клиентская часть)

| Контроллер | Путь | Описание |
|------------|------|----------|
| `SiteController` | `/` | Главная, курсы, поиск |
| `ArticlesController` | `/articles` | Статьи и категории |
| `CoursesMyController` | `/my-courses` | Мои курсы |
| `PricePlansController` | `/my-enrollments` | Зачисления/тарифы |
| `GuestController` | `/guest/*` | Регистрация, вход, восстановление |
| `DashboardController` | `/dashboard` | Личный кабинет |

### 4.2. Система роутинга

```php
// Основные паттерны:
'/articles'                          -> ArticlesController::actionIndex()
'/articles/{slug}'                   -> ArticlesController::actionCategory()
'/article/{slug}'                    -> ArticlesController::actionView()
'/courses'                           -> SiteController::actionCoursesIndex()
'/courses/{category}/{subcategory}'  -> SiteController::actionCourses()
'/my-courses'                        -> CoursesMyController::actionIndex()
'/dashboard'                         -> DashboardController::actionIndex()